<%# 
    이 부분은 회원정보를 수정하는 포맷이다.
    이름/이메일/비밀번호/비밀번호 확인으로 구성된다.
    또한 계정 삭제도 가능하다.
%>

<h2>회원정보 수정</h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  
  <%= devise_error_messages! %>

  <div class="panel panel-default">
    <div class="panel-body">
      <div class="form-inputs">
        <div class="form-group">
        <%= f.label :name, "이름" %>
          <%= f.text_field :name, class: "form-control", :autofocus => true %>
        </div>

        <div class="form-group">
          <%= f.label :email, "이메일" %>
          <%= f.email_field :email, class: "form-control", :autofocus => true %>
        </div>

        <div class="form-group">
          <%= f.label :password, "비밀번호" %> <small>(바꾸고 싶지 않다면, 빈칸으로 두시면 됩니다.)</small>
          <%= f.password_field :password, class: "form-control", :autocomplete => "off" %>
        </div>

        <div class="form-group">
          <%= f.label :current_password, "비밀번호 확인" %> <small>(회원정보를 바꾸기 위해서는 현재 비밀번호를 기입해주세요.)</small>
          <%= f.password_field :current_password, class: "form-control" %>
        </div>

        
      </div>
      <div class="form-group">
        <%= f.submit "수정하기", class: "btn btn-primary" %>
      </div>
    </div>
  </div>
<% end %>

<div class="panel-footer">
  <p><%= button_to "계정을 삭제합니다.", registration_path(resource_name), data: { confirm: "정말로 삭제하시겠습니까?" }, method: :delete, class: "btn btn-default" %></p>
</div>


